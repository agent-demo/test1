name: crop_saathi
description: Offline-first crop health screening and advisory prototype.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.4
  http: ^1.3.0
  image_picker: ^1.1.2
  path: ^1.9.0
  sqflite: ^2.4.1
  flutter_tts: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
